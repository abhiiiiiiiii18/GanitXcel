rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY - Allow all read/write for testing
    // ⚠️ WARNING: DO NOT USE IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
